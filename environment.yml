name: xbrl-extract
channels:
 - conda-forge
dependencies:
 - pip>=21.0,<22

 # Jupyter notebook specific packages
 - jupyterlab ~= 3.0
 - jellyfish
 - stringcase
 - scipy

 # XBRL parsing library
 - pip:
    - git+https://github.com/Arelle/Arelle.git@master
    - --editable ./
